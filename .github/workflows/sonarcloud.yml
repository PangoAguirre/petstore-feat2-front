name: SonarCloud Scan

on:
  push:
    branches: [ main, master ]
  pull_request:
    types: [ opened, synchronize, reopened ]

jobs:
  sonar:
    name: SonarCloud
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18.20.4'

      - name: Cache node modules
        uses: actions/cache@v3
        with:
          path: |
            **/node_modules
            ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}

      - name: Install dependencies
        run: npm ci

      - name: Run unit tests (frontend & backend)
        run: npm test -- --coverage

      - name: SonarCloud Scan
        uses: sonarsource/sonarcloud-github-action@v2
        with:
          projectKey: PangoAguirre_petstore-feat2-front
          organization: pangoaguirre
          token: 3e18148005a0555ca87f0df633d98b1f1a05a96b
          args: >
            -Dsonar.token=3e18148005a0555ca87f0df633d98b1f1a05a96b
            -Dsonar.projectKey=PangoAguirre_petstore-feat2-front
